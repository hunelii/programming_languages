% Define a vector x with values from 0 to 2*pi in steps of pi/4
% is the comment sign in matlab
x = 0:pi/4:2*pi;

% Compute the sine of each value in x
y = sin(x);

% Display the vector y
disp('The sine values are:');
disp(y);
